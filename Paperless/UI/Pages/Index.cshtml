@page
@model IndexModel
@{
    ViewData["Title"] = "Dashboard";
}
<div class="dashboard-content">
    <h1 class="dashboard-title">Dashboard</h1>
    
    <!-- Statistics Cards -->
    <div class="stats-grid">
        <div class="stat-card success">
            <div class="stat-title">Total Documents</div>
            <div class="stat-value" id="totalDocuments">-</div>
        </div>
        <div class="stat-card info">
            <div class="stat-title">Total Tags</div>
            <div class="stat-value" id="totalTags">-</div>
        </div>
        <div class="stat-card warning">
            <div class="stat-title">Recent Uploads</div>
            <div class="stat-value" id="recentUploads">-</div>
        </div>
        <div class="stat-card danger">
            <div class="stat-title">Access Logs</div>
            <div class="stat-value" id="accessLogs">-</div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="quick-actions">
        <h2 class="section-title">Quick Actions</h2>
        <div class="action-buttons">
            <a href="/Documents" class="action-button">
                <i class="fas fa-file-alt"></i>
                <span>View All Documents</span>
            </a>
            <button class="action-button" onclick="openUploadModal()">
                <i class="fas fa-upload"></i>
                <span>Upload Document</span>
            </button>
            <a href="/Tags" class="action-button">
                <i class="fas fa-tags"></i>
                <span>Manage Tags</span>
            </a>
        </div>
    </div>

</div>

<!-- Upload Modal -->
<div id="uploadModal" class="modal" style="display: none;">
    <div class="modal-content">
        <div class="modal-header">
            <h3>Upload Document</h3>
            <span class="close" onclick="closeUploadModal()">&times;</span>
        </div>
        <div class="modal-body">
            <form id="uploadForm" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="fileInput">Select File:</label>
                    <input type="file" id="fileInput" name="file" accept=".pdf,.doc,.docx,.txt,.jpg,.jpeg,.png" required>
                </div>
                <div class="form-group">
                    <label for="documentTitle">Document Title:</label>
                    <input type="text" id="documentTitle" name="title" placeholder="Enter document title">
                </div>
                <div class="form-group">
                    <label for="documentTags">Tags (comma-separated):</label>
                    <input type="text" id="documentTags" name="tags" placeholder="e.g., invoice, 2024, legal">
                </div>
                <div class="form-group">
                    <label for="documentSummary">Summary (optional):</label>
                    <textarea id="documentSummary" name="summary" rows="3" placeholder="Brief description of the document"></textarea>
                </div>
                <div class="modal-actions">
                    <button type="button" class="btn btn-secondary" onclick="closeUploadModal()">Cancel</button>
                    <button type="submit" class="btn btn-primary">Upload Document</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>

async function loadDashboard() {
    try {
        // Fetch documents
        const docsRes = await fetch('http://localhost:8081/api/document');
        const documents = await docsRes.json();
        
        // Fetch tags (assuming /api/tag exists, otherwise extract from docs)
        let tagsCount = 0;
        try {
            const tagsRes = await fetch('http://localhost:8081/api/tag'); // URL guess based on TagController existence
            if (tagsRes.ok) {
                 const tags = await tagsRes.json();
                 tagsCount = tags.length;
            }
        } catch (e) {
            console.warn("Could not fetch tags, using default", e);
        }

        // Set document count
        document.getElementById('totalDocuments').textContent = documents.length;
        
        // Set tags count
        document.getElementById('totalTags').textContent = tagsCount;
        
        // Calculate recent uploads (last 7 days)
        const weekAgo = new Date();
        weekAgo.setDate(weekAgo.getDate() - 7);
        const recentCount = documents.filter(doc => new Date(doc.uploadedAt) > weekAgo).length;
        document.getElementById('recentUploads').textContent = recentCount;
        
        // Set access logs (Placeholder as we don't have an endpoint for this statistic yet or need to calculate it)
        document.getElementById('accessLogs').textContent = "-";
        
        
    } catch (error) {
        console.error('Error loading dashboard:', error);
        document.getElementById('totalDocuments').textContent = 'Error';
        document.getElementById('totalTags').textContent = 'Error';
        document.getElementById('recentUploads').textContent = 'Error';
    }
}

// Upload modal functionality
function openUploadModal() {
    document.getElementById('uploadModal').style.display = 'block';
}

function closeUploadModal() {
    document.getElementById('uploadModal').style.display = 'none';
    document.getElementById('uploadForm').reset();
}

// Upload form submission
document.getElementById('uploadForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    
    const fileInput = document.getElementById('fileInput');
    const titleInput = document.getElementById('documentTitle');
    const tagsInput = document.getElementById('documentTags');
    const summaryInput = document.getElementById('documentSummary');
    
    if (!fileInput.files[0]) {
        alert('Please select a file to upload');
        return;
    }
    
    const formData = new FormData();
    formData.append('File', fileInput.files[0]);
    if (titleInput.value) formData.append('Title', titleInput.value);
    
    if (tagsInput.value) {
        const tags = tagsInput.value.split(',');
        tags.forEach(tag => formData.append('Tags', tag.trim()));
    }
    
    try {
        const response = await fetch('http://localhost:8081/api/document', {
            method: 'POST',
            body: formData
        });

        if (response.ok) {
            alert('Document uploaded successfully!');
            closeUploadModal();
            loadDashboard(); // Reload stats
        } else {
            const err = await response.text();
            alert('Upload failed: ' + err);
        }
    } catch (error) {
        console.error('Upload error:', error);
        alert('Upload failed: ' + error.message);
    }
});

// Close modal when clicking outside
window.onclick = function(event) {
    const modal = document.getElementById('uploadModal');
    if (event.target === modal) {
        closeUploadModal();
    }
}

loadDashboard();
</script>

<style>
body, .container-fluid { background: #222 !important; }
.card { border-radius: 12px; }
.badge { font-size: 0.9em; }

/* Upload Modal Styles */
.modal {
    position: fixed;
    z-index: 1000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    box-sizing: border-box;
}

.modal-content {
    background-color: #2c2c2c;
    border: 1px solid #404040;
    border-radius: 8px;
    width: 90%;
    max-width: 500px;
    max-height: 90vh;
    overflow-y: auto;
    margin: auto;
    position: relative;
}

.modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem;
    border-bottom: 1px solid #404040;
}

.modal-header h3 {
    margin: 0;
    color: #ffffff;
}

.close {
    color: #b0b0b0;
    font-size: 28px;
    font-weight: bold;
    cursor: pointer;
    line-height: 1;
}

.close:hover {
    color: #ffffff;
}

.modal-body {
    padding: 1rem;
}

.form-group {
    margin-bottom: 1rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: #ffffff;
    font-weight: 500;
}

.form-group input,
.form-group textarea {
    width: 100%;
    padding: 0.5rem;
    border: 1px solid #404040;
    border-radius: 4px;
    background-color: #1a1a1a;
    color: #ffffff;
    font-size: 14px;
}

.form-group input:focus,
.form-group textarea:focus {
    outline: none;
    border-color: #28a745;
}

.modal-actions {
    display: flex;
    justify-content: flex-end;
    gap: 0.5rem;
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid #404040;
}

.btn {
    padding: 0.5rem 1rem;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 14px;
    font-weight: 500;
    text-decoration: none;
    display: inline-block;
    text-align: center;
}

.btn-primary {
    background-color: #28a745;
    color: #ffffff;
}

.btn-primary:hover {
    background-color: #218838;
}

.btn-secondary {
    background-color: #6c757d;
    color: #ffffff;
}

.btn-secondary:hover {
    background-color: #5a6268;
}
</style>